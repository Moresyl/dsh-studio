# 更新日志

本项目所有值得记录的变更都写在这里。格式遵循
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/lang/zh-CN/)——但要带上 1.0 之前的那条惯例：
任何东西都还可能动。

[English](CHANGELOG.md)

## [未发布]

### 新增

- **有新版本时会自己说一声。** 窗口在启动几秒后、以及此后每六个小时
  去问一次 release feed；一旦发布的版本比正在跑的这个新，
  左下角就会出现一条提示：一行字、点一下打开发布页、旁边一个关闭按钮。
  「暂不提醒」是按版本记的，而且能跨重启记住——
  对 0.3.0 说过的「暂不」，会一直算数到 0.4.0 出现为止。
  不下载任何东西，也不安装任何东西。
  导航栏的「关于」在新版本还在的期间会一直带一个小圆点，
  所以这条提示并不是回到它的唯一入口。

### 变更

- **服务地址从底部状态栏移走了。** 一个在窗口最下面印着 `127.0.0.1:52418`
  的桌面软件，等于自己把底细说了出来。地址是关于「管道」的事实，
  所以它现在只留在控制台的服务一栏里——在那里依然左键点击用浏览器打开、
  右键复制。
- 检查更新的结果由状态栏和「关于」面板共用，不再各查一次；
  并且检查失败不再写进 harness 日志——笔记本在火车上没网，
  不是一件值得上报的事。

## [0.2.0] —— 2026-08-16

两个改变「这个外壳是干什么用的」的功能：它现在可以扩展 harness，
也可以把 harness 交到你手机上。

### 新增

- **插件市场。** 在窗口里直接搜索 npm registry，
  在决定装之前先看清这个包声明了什么——版本、许可、依赖，
  以及它究竟带不带一个 profile 补丁——然后装进 harness 托管的 profile。
  安装和卸载走的是 harness 自己的插件命令，而不是背着它去改它的文件，
  所以结果和 harness 自己装出来的完全一致。
  没有 profile 补丁的包会如实标成「库」，
  而不是装完之后表现得像一个悄无声息什么都没干的插件；
  随 profile 模板一起来的那些则标为内置，不能从这里卸载。
- **手机远程访问。** 默认关闭，需要你亲手打开；打开也不会挪动服务——
  `dsh` 始终待在回环上。打开的是另一个网关：只绑定选定的那一个局域网地址，
  手里攥着这次会话现铸的 128 位令牌，且该令牌从不落盘、也不进日志。
  配对靠二维码，由 Rust 侧编码——扫一下，令牌换成 cookie，
  之后的一切原样拼接到 harness。
  面板会报出当前有几条连接活着、一共服务过多少次、拒绝过多少次。
  关掉这扇门，或者 harness 停掉，令牌立即作废；下一次会话铸的是另一个。
- **检查更新。** 「关于」面板会去问发布源最新发布的版本是哪个，
  并告诉你它是不是比正在跑的这个新。不下载任何东西，也不安装任何东西——
  给出的答案是一个链接。
- **「关于」面板。** 版本、平台与架构，以及应用真正会用到的三个目录，
  每一个都可以点开到文件管理器。
- **从单一面板变成工作台。** 控制面板拆成四个视图——控制台、插件、远程、关于——
  由一条导航栏承载，而这条栏本身带着实时状态：远程开着时亮一个点，
  以及已安装插件的数量。切到 harness 时视图状态不丢，
  所以打了一半的搜索词和正显示着的配对码都能活过「去看一眼 harness」。

### 修复

- Unix 下的进程树测试在写夹具脚本时没有给可执行位，
  导致「`proc-guard` 能收掉孙子进程」这条用例在 Linux 和 macOS 上根本跑不起来。

### 文档

- 新增 `CONTRIBUTING.md`、`SECURITY.md`、本更新日志，
  以及 GitHub 的 issue 与 PR 模板，每一份都有中文版。
- README 补上了架构图、逐步拆开的启动流程、四个发布目标的安装表、
  每个设计决定各自放弃了什么，以及常见问题。
- `SECURITY.md` 写明了远程网关保证的是什么——默认关闭、只绑定一个地址、
  没有会话令牌就不转发、令牌不落盘——好让相关的漏洞报告有一个可对照的标准。

## [0.1.1] —— 2026-08-15

### 修复

- `devDependencies` 里漏了 `@types/node`，于是 `tsc --noEmit`——
  连带 `pnpm build` 和发布流水线——在干净检出上会失败，尽管本地是通过的。
  应用的行为没有任何变化。

## [0.1.0] —— 2026-08-15

首个版本。在 Windows 11 上端到端验证通过；macOS 与 Linux 由 CI 构建，
但还没有人真正跑起来看过。

### 新增

- **Node 检测。** 用 `--version` 逐个探测机器上的每一个 Node，
  包括版本管理器装了但从没进过 `PATH` 的那些。满足最低版本要求的里面最新的胜出；
  两个目录报出同一版本时按路径定序，保证每次启动选出来的都是同一个。
- **一键安装。** `@deepseek-ai/dsh` 不在时，那一行提示本身就是按钮。
  它装进应用数据目录下的私有 prefix，直接用检测到的 Node 可执行文件调用
  `npm-cli.js`、全程不经过 shell，并把输出实时打进窗口里。
- **Supervisor。** 服务运行在 supervisor 之下，退出后按退避策略重启。
- **端口由内核分配。** 服务以 `--port 0` 启动，supervisor 再从就绪行里
  把它实际绑定到的端口读回来。既没有配置好的端口可冲突，
  也不存在「先检查端口、再绑定端口」中间的那段空隙。
- **健康探测。** 每 10 秒发一次真正的 HTTP 请求——
  因为一个卡死的服务，PID 仍然是活的，TCP 连接也仍然能成功，
  握手是内核从 listen backlog 里替它完成的。连续三次没有回应就回收重启。
- **进程树回收。** Windows 上服务被放进带 `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`
  的 Job Object，就算外壳自己被强杀，内核也会把整棵树收掉；
  Unix 上则单独建进程组、按组发信号。
- **承载 harness。** 上游界面从它自己的源加载在外壳标题栏下方的 frame 里。
  切回控制面板不会丢掉正在进行的会话，上游也没有被打补丁或 vendor 进来。
- **日志控制台。** 外壳自身的输出，可选中、可复制。
- **中英双语，** 跟随系统语言。
- **托盘图标**，带「打开 / 退出」菜单；服务运行期间关闭窗口是收进托盘，
  免得一次误关就结束了一整个会话。
- **桌面软件形态的窗口**——自绘标题栏带最小化、最大化与关闭，
  底部状态栏承载 harness 当前在做什么、服务地址、选中的 Node 与工作区。
  地址左键点击用浏览器打开、右键复制，工作区点击则在文件管理器里打开。
- **发布流水线。** 打了 tag 的版本由 CI 构建 Windows x64、Linux x64、
  macOS Apple Silicon 与 macOS Intel 四个目标。

[未发布]: https://github.com/Moresyl/dsh-studio/compare/v0.2.0...HEAD
[0.2.0]: https://github.com/Moresyl/dsh-studio/compare/v0.1.1...v0.2.0
[0.1.1]: https://github.com/Moresyl/dsh-studio/compare/v0.1.0...v0.1.1
[0.1.0]: https://github.com/Moresyl/dsh-studio/releases/tag/v0.1.0
